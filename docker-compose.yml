services:
  frontend:
    build: .
    image: zerowaiting_fronted
    container_name: frontend
    ports:
      - 3000:3000
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
    restart: unless-stopped
